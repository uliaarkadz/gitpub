<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <div id="main">
      <h1>At foo bar</h1>
      <%if(locals.drink){%>
      <h2><%=drink.name%></h2>
      <% const drinkimage = drink.image + ".png"%>
      <div id="coctail"><img src="<%=drinkimage%>" /></div>
      <h3><%=drink.price%></h3>
      <%} else if(locals.item){%>
      <h2><%=item.name%></h2>
      <% const foodimage = item.image + ".png"%>
      <div id="coctail"><img src="<%=foodimage%>" /></div>
      <h3><%=item.price%></h3>
      <%}%>
      <div id="home"><a href="/drinks">Back to Menu</a></div>
    </div>
  </body>
  <style>
    body {
      background-color: steelblue;
    }
    h1 {
      color: blue;
    }
    h2,
    h3 {
      color: rgb(208, 23, 137);
    }
    img {
      width: 100%;
      height: 100%;
    }
    #main {
      display: flex;
      flex-direction: column;
      align-items: center;
    }
    #coctail {
      width: 30vw;
    }
  </style>
</html>
